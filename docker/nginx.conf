upstream django_backend {
    server web:8000;
}

upstream nextjs_frontend {
    server frontend:3000;
}

server {
    listen 80;
    server_name localhost;

    # Allow large file uploads (match Django's 100MB limit)
    client_max_body_size 110M;

    # Django API, admin, and WebSocket routes
    location ~ ^/(api|admin|static|ws)/ {
        proxy_pass http://django_backend;
        proxy_http_version 1.1;

        # These two headers are required for WebSocket upgrades
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";

        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_read_timeout 120s;
    }

    # Everything else goes to Next.js
    location / {
        proxy_pass http://nextjs_frontend;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
```

### Step 41 — Create .env file for Docker
Create `C:\rigflow\.env`:
```
DB_PASSWORD=your_strong_database_password
SECRET_KEY=your-very-long-random-secret-key-here
REDIS_URL=redis://redis:6379/0

# AWS S3 (leave blank for local dev — uses local filesystem storage)
AWS_BUCKET_NAME=
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=

# Stripe (leave blank until you set up payments)
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=